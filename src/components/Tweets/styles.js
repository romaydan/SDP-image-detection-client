import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  card: {
    height: 'auto',
    marginBottom: '7px',
  },
  tweetsContainer: {
    display: 'flex',
    flexDirection: 'column-reverse',
  },
});
